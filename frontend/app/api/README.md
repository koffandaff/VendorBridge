# app/api

Next.js Route Handlers (server-side endpoints in the frontend).

Intentionally empty. Add handlers here only if the frontend genuinely needs a server-side endpoint (e.g. proxying to the backend).
