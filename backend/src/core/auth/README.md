# core/auth

Authentication infrastructure (sessions, tokens, guards) will live here.

Intentionally empty. Authentication is not added yet by design - it will be introduced with the first feature that requires it.
